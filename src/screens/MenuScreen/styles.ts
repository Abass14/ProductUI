import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingTop: 20,
    flex: 1,
    gap: 20,
    paddingBottom: 20,
  },
  productItems: {
    flexBasis: '47%',
    marginLeft: 5,
    marginRight: 5,
  },
  flatlist: {
    // flexDirection: 'row',
    gap: 10,
  },
});
